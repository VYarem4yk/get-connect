let initialState = {};

let authReducer = (state = initialState, action) => {
  switch (action.type) {
    case SET: {
      let st
      return stateCopy;
    }

    default:
      return state;
  }
};
export default authReducer;
