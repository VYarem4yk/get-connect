let initialState = {

};

let authReducer = (state = initialState, action) => {
  switch (action.type) {
    case SET :{
      return stateCopy;
    }
    }
  }
  return state;

};

export default authReducer;
